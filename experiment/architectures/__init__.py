"""The four benchmarked architectures.

A0 rule-only (baseline) and A2 (rule + current LLM fallback) run through the
production path; A1 (LLM as primary extractor) and A3 (rule-first + LLM
precision gate) are implemented here.
"""
